[project]
name = "dbt-project"
version = "0.1.0"
description = "dbt transformations for the Olist ELT platform"
readme = "README.md"
requires-python = ">=3.11,<3.14"
dependencies = [
    "dbt-core>=1.11.12",
    "dbt-postgres>=1.10.2",
]

[tool.uv.workspace]
members = []
