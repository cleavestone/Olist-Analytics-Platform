[project]
name = "dlt-pipeline"
version = "0.1.0"
description = "dlt ingestion pipeline for the Olist ELT platform"
readme = "README.md"
requires-python = ">=3.11,<3.14"
dependencies = [
    "dlt[postgres]>=1.28.1",
    "requests>=2.34.2",
]

[tool.uv.workspace]
members = []
